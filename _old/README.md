# Archive — direction bleu/ambre (v1)

Ces fichiers sont l'ancienne version multi-pages du site (charte institutionnelle
bleu + ambre). Ils ont été remplacés le 2026-07-05 par la nouvelle direction
one-page sombre/teal (`../index.html`).

Conservés pour référence. Chaque page embarque son propre `colors_and_type.css`,
`_site.css` et `_site.js` (copies de la v1) pour rester consultable hors ligne.
Le dossier `assets/` (favicon) n'est pas dupliqué ici.
